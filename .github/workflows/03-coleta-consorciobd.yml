name: 03 · Coleta ConsorcioBD

on:
  workflow_dispatch:
  schedule:
    # 06:00 BRT = 09:00 UTC; janela mensal do BCB.
    - cron: "0 9 12 * *"

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

concurrency:
  group: coleta-consorciobd
  cancel-in-progress: false

jobs:
  collect:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: "pip"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Ensure runtime directories
        run: mkdir -p data/raw/bc/consorciobd data/stage/consorciobd data/runtime data/source_state
      - name: Run collector
        id: collect
        run: |
          python collectors/bc_consorciobd.py \
            --config config/sources.json \
            --source bc_consorciobd
      - name: Persist source check state
        if: always()
        run: |
          python scripts/persist_source_state.py \
            --source bc_consorciobd \
            --runtime data/runtime/bc_consorciobd.json \
            --snapshot data/raw/bc/consorciobd/latest.json \
            --content-file data/raw/bc/consorciobd/latest_source.bin \
            --state data/source_state/bc_consorciobd.json \
            --collector-outcome "${{ steps.collect.outcome }}" \
            --accepted-mode direct-content-download \
            --competence-mode auto
      - name: Collect official segment taxonomy
        id: taxonomy
        if: steps.collect.outcome == 'success'
        run: |
          python collectors/bc_segment_taxonomy.py \
            --config config/sources.json \
            --source bc_consorciobd
      - name: Commit approved data, taxonomy and source state
        if: always()
        shell: bash
        run: |
          set -Eeuo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -f data/source_state/bc_consorciobd.json
          if [ "${{ steps.collect.outcome }}" = "success" ] && [ "${{ steps.collect.outputs.mode_used }}" = "direct-content-download" ]; then
            git add -f data/raw/bc/consorciobd/latest.json data/raw/bc/consorciobd/latest_source.bin data/stage/consorciobd/consorciobd_catalog.json
          fi
          if [ "${{ steps.taxonomy.outcome }}" = "success" ]; then
            git add -f data/stage/consorciobd/segment_taxonomy.json
          fi
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "chore(data): registra ConsorcioBD, taxonomia oficial e estado da fonte"
          git pull --rebase origin "$GITHUB_REF_NAME"
          git push origin "HEAD:$GITHUB_REF_NAME"
      - name: Enforce homologated mode
        if: always() && steps.collect.outcome == 'success' && steps.collect.outputs.mode_used != 'direct-content-download'
        run: exit 1
      - name: Enforce additive taxonomy policy
        if: always() && steps.taxonomy.outcome == 'success' && steps.taxonomy.outputs.status == 'structural_change_detected'
        run: |
          echo "BCB alterou/removou significado de segmento existente; evidência foi registrada, mas a migração exige revisão humana." >&2
          exit 1
