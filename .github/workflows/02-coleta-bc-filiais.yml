name: 02 · Coleta BC Filiais

on:
  workflow_dispatch:
  schedule:
    # 09:20 BRT = 12:20 UTC
    - cron: "20 12 * * *"

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

concurrency:
  group: coleta-bc-filiais
  cancel-in-progress: false

jobs:
  collect:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Ensure runtime directories
        run: |
          mkdir -p data/raw/bc/filiais
          mkdir -p data/stage/filiais
          mkdir -p data/runtime

      - name: Run collector
        id: collect
        run: |
          python collectors/bc_filiais.py \
            --config config/sources.json \
            --source bc_filiais

      - name: Upload collector outputs
        if: success()
        uses: actions/upload-artifact@v6
        with:
          name: bc-filiais-output
          path: |
            data/raw/bc/filiais/latest.json
            data/stage/filiais/filiais.json
            data/runtime/bc_filiais.json
          if-no-files-found: warn
          retention-days: 15

      - name: Commit changed outputs
        if: success() && steps.collect.outputs.changed == 'true' && steps.collect.outputs.mode_used == 'odata-csv'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add -f \
            data/raw/bc/filiais/latest.json \
            data/stage/filiais/filiais.json

          if git diff --cached --quiet; then
            echo "Sem diff rastreável para commit."
            exit 0
          fi

          git commit -m "chore(data): atualiza filiais BC"
          git push
